{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "osd",
  "description": "Minimal capability for the OSD popup window",
  "windows": ["osd"],
  "permissions": [
    "core:default"
  ]
}
